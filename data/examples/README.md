# data/examples

Scratch space for small hand-made inputs (e.g. an external 8x2 codebook `.npz`
to attach via `data/codebooks/`, or reference patterns). Nothing here is required
to run the evaluation; the bundled codebooks are generated deterministically on
first use and cached under `../codebooks/`.
